<UserControl x:Class="JobOverview.View.UCProgressBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:JobOverview.View"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <StackPanel DockPanel.Dock="Top">
            <Label x:Name="lblProgress" Content="{Binding TextToDisplay}"/>
            <ProgressBar x:Name="progress" Height="25" IsIndeterminate="False" Value="{Binding Progress}"></ProgressBar>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
